#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @na: the first integer
 * @nb: the secong integer
 * Return: Always (Success)
 */
int add(int na, int nb)
{
	return (na + nb);
}
